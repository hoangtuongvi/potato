interface:
  display_name: "Potato"
  short_description: "Assistant infrastructure for Codex"
  default_prompt: "Use $potato to work through this with the Potato algorithm: observe, think, plan, build, execute, verify, learn, and summarize."
policy:
  allow_implicit_invocation: true
